1. UN SOLO UTENTE crea le stanze per tutti
2. Ogni utente dev'essere inserito "A MANO" nella stanza. Ciò avviene:
	2a. in CREAZIONE/MODIFICA della stanza (impostando i vari strumenti)
	2b. all'accesso SE la stanza è pubblica e l'utente non è già inserito nella stanza
  LA MAIL è chiave univoca dell'utente per quella stanza
